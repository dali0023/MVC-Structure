const fs = require("fs");

class Product {
  constructor(title) {
    this.title = title;
  }
  // Save Data
  save() {}

  // Update Data
  update() {}

  // delete Data
  delete() {}

  // Display / Show Data
  static fetchAll() {}
}

module.exports = Product;
